# Copy this file to .env and edit before running docker compose (prod).
# The server image reads PORT; the TURN_* vars are used by coturn in dc.prod.yml.

PORT=8080

# TURN server credentials — must match the ICE server config in peer.js / stream.js.
TURN_USER=user
TURN_PASS=password
TURN_REALM=videochat
